from setuptools import setup

setup(name='radiotool',
      version='0.1',
      description='Audio tools for radio production',
      url='https://bitbucket.org/srubin/radiotool',
      author='Steve Rubin',
      author_email='srubin@cs.berkeley.edu',
      packages=['radiotool'],
      install_requires=[
      ],
      zip_safe=False)